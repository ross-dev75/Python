# oop

Object-oriented programming practice.

## Projects

- `coffee-maker/` — OOP refactor of the procedural coffee machine. Splits logic into three classes (`CoffeeMaker`, `MoneyMachine`, `Menu`) with a thin `main.py` orchestrator.
