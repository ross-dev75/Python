# data

CSV data wrangling and file I/O exercises.

## Contents

- `mail-merge/mail_merge.py` — reads a name list and a letter template, outputs one personalized letter file per name. Paths are placeholders; update the constants at the top before running.
- `weather_data.csv` — sample weather dataset used for pandas practice (no corresponding script committed).
